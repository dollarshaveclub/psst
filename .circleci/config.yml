version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.10.3
    working_directory: /go/src/github.com/dollarshaveclub/psst
    steps:
      - checkout
      - run:
          name: Run tests
          command: make test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build